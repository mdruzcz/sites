{
  "name": "turnstile-verify",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Cloudflare Worker that verifies Turnstile tokens for all Master Decker sites.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:set": "wrangler secret put TURNSTILE_SECRET_KEY",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
