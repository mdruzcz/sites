{
  "name": "forms-submit",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Cloudflare Worker that inserts lead/quote form rows into Supabase for all Master Decker sites, holding the single Supabase publishable key.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:set": "wrangler secret put SUPABASE_KEY",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
