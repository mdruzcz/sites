# Shared LCF Supabase project (multi-store ecom data)
NEXT_PUBLIC_SUPABASE_URL=https://symgxmokposzjcgikgnz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only — never expose
SUPABASE_SERVICE_ROLE_KEY=

# Auth redirect base (production: https://admin.masterdecker.com)
NEXT_PUBLIC_SITE_URL=http://localhost:3100

# Default storefront domains for revalidate webhooks. Per-store secrets live in ecom_stores.revalidation_secret_id (Vault).
